@model string

@{
    var color = ColorHelper.GetProjectColor(Model);
}

@if (!string.IsNullOrEmpty(Model))
{
    <span class="badge rounded-pill shadow-sm"
          style="background-color: @color; min-width: 45px; letter-spacing: 0.5px;">
        @Model.ToUpper()
    </span>
}